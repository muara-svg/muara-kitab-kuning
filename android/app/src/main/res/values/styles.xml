<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>

        <!-- ================= STABILISASI BAR NOTIFIKASI ================= -->
        <!-- 1. Menyetel latar belakang Status Bar agar menyatu dengan Header hijau MUARA -->
        <item name="android:statusBarColor">#024129</item>

        <!-- 2. Menghidupkan ikon & teks berwarna putih agar terbaca jelas di atas warna gelap -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- 3. Opsional: Menyetel warna bilah navigasi bawah HP agar serasi -->
        <item name="android:navigationBarColor">#fafafa</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>

        <!-- Menyelaraskan Status Bar saat aplikasi pertama kali melakukan loading/splash -->
        <item name="android:statusBarColor">#024129</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>